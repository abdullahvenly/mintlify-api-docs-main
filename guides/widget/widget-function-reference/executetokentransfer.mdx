---
title: "executeTokenTransfer"
slug: "executetokentransfer"
excerpt: "Transfer a fungible token from one destination to another."
hidden: false
createdAt: "Mon Oct 16 2023 14:30:32 GMT+0000 (Coordinated Universal Time)"
updatedAt: "Tue Jan 02 2024 09:42:39 GMT+0000 (Coordinated Universal Time)"
---
This function transfers fungible tokens (ex. ERC20/TRC20/VIP180/NEP5/GO20/…​) from one address to another. The destination can be any blockchain address, a wallet, or a smart contract, it can even be an email address.

```javascript
venlyConnect.signer.executeTokenTransfer({
    walletId: '71dec640-4eb8-4321-adb8-b79461573fc4',
    to: '0xf147cA0b981C0CD0955D1323DB9980F4B43e9FED',
    value: 1010,
    tokenAddress: '0x02f96ef85cad6639500ca1cc8356f0b5ca5bf1d2'
    secretType: 'ETHEREUM',
    })
```

> 📘 🧙 You don’t have to take into account the number of decimals for different tokens, Venly handles that for you.
> 
> **Example:** If a token has 18 decimals and you want to transfer 1 token, provide the value 1. Venly will translate this to the correct non-decimal value (1 \* 10e18).

### Signature:

```javascript
signer.executeTokenTransfer(tokenTransferRequestDto, options?): Promise<SignerResult>
```

### Returns:

```javascript
Promise<SignerResult>
```

### Parameters:

| Parameter                                                                         | Required | Description                                                                                                                                                                           |
| --------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`tokenTransferRequestDto`](https://venly.readme.io/docs/tokentransferrequestdto) | `True`   | The transfer request you want to execute. For more info on how this request should look like, see [`tokenTransferRequestDto`](https://venly.readme.io/docs/tokentransferrequestdto) . |
| [`options`](https://venly.readme.io/docs/redirectoptions)                         | `False`  | Redirect options you want to pass. Only available when using a REDIRECT [`signer`](https://venly.readme.io/docs/signer)                                                               |

### Example:

```javascript
venlyConnect.signer.executeTokenTransfer({
    walletId: '71dec640-4eb8-4321-adb8-b79461573fc4',
    to: '0xf147cA0b981C0CD0955D1323DB9980F4B43e9FED',
    value: 1010,
    tokenAddress: '0x02f96ef85cad6639500ca1cc8356f0b5ca5bf1d2'
    secretType: 'ETHEREUM',
}).then((signerResult) => {
    if (signerResult.success) {
        console.log(`Transaction ${signerResult.result.transactionHash} has been successfully executed!`);
    } else {
        console.warn(`Something went wrong while executing the transaction`);
    }
}).catch((reason) => {
    console.log(error);
});
```

> 📘 🧙 Using the `network` parameter, the node to which the transaction is sent can be set manually.  It allows you to submit a transaction to any mainnet or testnet node of your choosing, public or private. ( Ethereum only)

## Object Types

> 📘 - [Signer](https://venly.readme.io/docs/signer)
> - [TokenTransferRequestDto](https://venly.readme.io/docs/tokentransferrequestdto)
> - [RedirectOptions](https://venly.readme.io/docs/redirectoptions)
> - [SignerResult](https://venly.readme.io/docs/signerresult)
